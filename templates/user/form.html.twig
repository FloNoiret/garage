{% extends "base.html.twig" %}

{% block body %}
	
	<h1 class="text-center mt-5"> Ajouter un Employé </h1>
	<div class="container mt-5 mb-5">
		{{ form_start(form) }}
		{{ form_rest(form) }}
		<input type="submit" value="Créer" class="btn btn-primary mt-2">
		{{ form_end(form) }}
	</div>
{% endblock %}
